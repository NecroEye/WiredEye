uniform float2 iResolution;
uniform float iTime;

half4 main(float2 fragCoord) {
  float2 uv = fragCoord / iResolution.xy;
  float v = 0.5 + 0.5 * sin((uv.x * 10.0) + iTime) * cos((uv.y * 10.0) - iTime);
  return half4(v, uv.x, uv.y, 1.0);
}